import React from 'react';

// This component is currently unused in the new corporate layout.
// Returning null to keep the file system clean without errors if imported.
export const TechSpecs: React.FC = () => {
  return null;
};